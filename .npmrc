frozen-lockfile=true
engine-strict=true

# GitHub Packages registry for the @cuweb scope.
# Auth token must be in a user-level ~/.npmrc or set via NODE_AUTH_TOKEN env var:
#   //npm.pkg.github.com/:_authToken=<your-PAT-with-read:packages>
@cuweb:registry=https://npm.pkg.github.com
