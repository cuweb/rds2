import * as react_jsx_runtime from 'react/jsx-runtime';
import React from 'react';

interface MainProps {
    children: React.ReactNode;
    hasPadding?: boolean;
}
declare const Main: ({ children, hasPadding }: MainProps) => react_jsx_runtime.JSX.Element;

export { Main };
